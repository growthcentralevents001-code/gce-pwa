name: Supabase DB Migrations

on:
  push:
    branches: [ development ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  migrate-dev:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/development'
    environment: development
    steps:
      - uses: actions/checkout@v4

      - name: Install PostgreSQL client
        run: |
          sudo apt-get update
          sudo apt-get install -y postgresql-client

      - name: Apply migrations to Dev
        env:
          DEV_DB_URL: ${{ secrets.DEV_DB_URL }}
        run: |
          echo "Connecting to Dev database using pooler connection..."
          URL="$DEV_DB_URL"
          
          # Extract connection parameters from URL
          USER=$(echo "$URL" | sed -n 's/.*:\/\/\([^:]*\):.*/\1/p')
          PASS_RAW=$(echo "$URL" | sed -n 's/.*:\([^@]*\)@.*/\1/p')
          PASS=$(echo "$PASS_RAW" | sed 's/%40/@/g')
          HOST=$(echo "$URL" | sed -n 's/.*@\([^:]*\):.*/\1/p')
          DB=$(echo "$URL" | sed -n 's/.*\/\([^?]*\).*/\1/p')
          PORT=5432
          
          echo "Database: $DB, Host: $HOST, Port: $PORT, User: $USER"
          
          if [ ! -d "supabase/migrations" ]; then
            echo "❌ ERROR: supabase/migrations directory not found!"
            exit 1
          fi
          
          MIGRATION_COUNT=$(find supabase/migrations -name "*.sql" | wc -l)
          if [ $MIGRATION_COUNT -eq 0 ]; then
            echo "⚠️ WARNING: No SQL migration files found in supabase/migrations"
            exit 0
          fi
          
          echo "Found $MIGRATION_COUNT migration file(s). Applying..."
          for file in supabase/migrations/*.sql; do
            echo "Applying $file"
            PGPASSWORD="$PASS" psql -h "$HOST" -U "$USER" -d "$DB" -p "$PORT" -f "$file" || {
              echo "❌ FAILED to apply migration: $file"
              exit 1
            }
            echo "✅ Successfully applied: $file"
          done

      - name: Verify Dev migration
        run: echo "✅ All Dev migrations applied successfully"

  migrate-prod:
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    environment: production
    needs: migrate-dev
    steps:
      - uses: actions/checkout@v4

      - name: Install PostgreSQL client
        run: |
          sudo apt-get update
          sudo apt-get install -y postgresql-client

      - name: Apply migrations to Prod
        env:
          PROD_DB_URL: ${{ secrets.PROD_DB_URL }}
        run: |
          echo "Connecting to Prod database using pooler connection..."
          URL="$PROD_DB_URL"
          
          # Extract connection parameters from URL
          USER=$(echo "$URL" | sed -n 's/.*:\/\/\([^:]*\):.*/\1/p')
          PASS_RAW=$(echo "$URL" | sed -n 's/.*:\([^@]*\)@.*/\1/p')
          PASS=$(echo "$PASS_RAW" | sed 's/%40/@/g')
          HOST=$(echo "$URL" | sed -n 's/.*@\([^:]*\):.*/\1/p')
          DB=$(echo "$URL" | sed -n 's/.*\/\([^?]*\).*/\1/p')
          PORT=5432
          
          echo "Database: $DB, Host: $HOST, Port: $PORT, User: $USER"
          
          if [ ! -d "supabase/migrations" ]; then
            echo "❌ ERROR: supabase/migrations directory not found!"
            exit 1
          fi
          
          MIGRATION_COUNT=$(find supabase/migrations -name "*.sql" | wc -l)
          if [ $MIGRATION_COUNT -eq 0 ]; then
            echo "⚠️ WARNING: No SQL migration files found in supabase/migrations"
            exit 0
          fi
          
          echo "Found $MIGRATION_COUNT migration file(s). Applying..."
          for file in supabase/migrations/*.sql; do
            echo "Applying $file"
            PGPASSWORD="$PASS" psql -h "$HOST" -U "$USER" -d "$DB" -p "$PORT" -f "$file" || {
              echo "❌ FAILED to apply migration: $file"
              exit 1
            }
            echo "✅ Successfully applied: $file"
          done

      - name: Verify Prod migration
        run: echo "✅ All Prod migrations applied successfully"
